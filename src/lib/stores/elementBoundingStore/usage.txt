<script lang="ts">
    import { elementBoundingStore } from 'svelte-legos';

    let el = $state(null);
    let rect = $derived(elementBoundingStore(el));

    const { x, y, top, right, bottom, left, width, height } = rect ?? {};
</script>

<div bind:this={el} />